// Not used currently. Can be removed if we don't need it.

Ext.define('ZCS.view.mail.ZtConvToolbar', {
	extend: 'Ext.TitleBar',
	xtype: 'convtoolbar',
	config: {
		docked: 'top',
		ui: 'light',
/*
		items: [
			{
				xtype: 'button',
//				handler: function() {
//					this.up('convtoolbar').fireEvent('showConvMenu');
//				},
				iconCls: 'arrow_down',
				iconMask: true,
				align: 'right'
			}
		],
*/
	}
});
